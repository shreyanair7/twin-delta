import { createFileRoute } from "@tanstack/react-router";
import { SCENARIOS, useDemoMode } from "@/lib/market/demo-mode";
export const Route=createFileRoute("/_authenticated/settings")({component:Settings});
function Settings(){const {scenario,setScenario}=useDemoMode();return <div className="max-w-3xl space-y-8"><div><p className="text-xs uppercase tracking-[.24em] text-muted-foreground">Preferences</p><h1 className="mt-3 text-4xl font-semibold">Settings</h1></div><section className="panel p-6"><h2 className="text-lg font-semibold">Demo mode</h2><p className="mt-2 text-sm text-muted-foreground">These scenarios drive the existing centralized demo market source. They do not pretend to be production market data.</p><div className="mt-6 grid gap-3 sm:grid-cols-2">{SCENARIOS.map(s=><button key={s.id} onClick={()=>setScenario(s.id)} className={`rounded-xl border p-4 text-left transition ${scenario===s.id?"border-primary/60 bg-primary/8":"border-border bg-secondary/20 hover:bg-secondary/50"}`}><p className="font-medium">{s.label}</p><p className="mt-1 text-xs text-muted-foreground">{s.description}</p></button>)}</div></section></div>}
